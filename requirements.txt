# Core
python-dotenv==1.0.1

# PDF parsing + table extraction
pdfplumber==0.11.4

# Local embeddings (open-source, runs on your machine, no API needed)
sentence-transformers==3.0.1

# Local vector database (no server needed, just a folder on disk)
chromadb==0.5.5

# Gemini API (used ONLY for final answer generation)
google-generativeai==0.8.3

# Token counting for smart chunking
tiktoken==0.7.0

# Misc utilities
tqdm==4.66.5
numpy==1.26.4
